Libraries
==========

We store packages of libraries we use. From these packages we do not want to allow access over the webserver, but we have to provide access to packages.
An example could be the library of a slider package including examples.
